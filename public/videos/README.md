# Local demo videos for `/watch`

These files are served from the Next.js `public` folder as stable local assets
(e.g. `/videos/demo-1.mp4`). The watch feed must not rely on remote media URLs.

| File | Source (downloaded once for demos) |
|------|-------------------------------------|
| `demo-1.mp4` | MDN CC0 sample (`flower.mp4`) |
| `demo-2.mp4` | W3Schools sample (`mov_bbb.mp4`) |
| `demo-3.mp4` | FileSamples sample (`sample_640x360.mp4`) |

Replace with UMTUBA creator uploads when the short-video pipeline ships.
