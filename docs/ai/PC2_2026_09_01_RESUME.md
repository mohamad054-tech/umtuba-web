# PC2 — 2026-09-01 resume

> **Not the owner’s saved stop.** Owner authority is **31 August** (`docs/ai/PC2_2026_08_31_END_OF_DAY_PRESERVATION.md`). Do not start from this file as the checkpoint. The next named gate here still needs an explicit GO.

```text
TASK_ID = CENTRAL_MIGRATION_VERSION_REALLOCATION_THEN_SOURCE_MERGE_GO
STATUS = NOT_STARTED
PREVIOUS_TASK = CENTRAL_UMTUBA_SOCIAL_COMM_PROFILE_INTEGRATION_REVIEW_V1
PREVIOUS_STATUS = SOURCE_PASS_MIGRATION_HOLD
DEVICE = PC2
DATE = 2026-09-01
YESTERDAY = 2026-08-31
PRODUCTION_TOUCHED = NO
PUSH = NO
MERGE = NO
```

A new agent starts here. Do **not** ask routine “where are we?” questions. Yesterday’s product and review work is finished.

Preservation: `docs/ai/PC2_2026_08_31_END_OF_DAY_PRESERVATION.md`

---

## Owner (first 5 minutes)

1. Boot the HP Z440. Docker Desktop must come up (WSL2 backend already installed).
2. Do **not** change BIOS. VTx is already working.
3. Do **not** push. Do **not** apply hosted migrations. Do **not** open production Studio to write.
4. Tell the next agent: continue from this file + last night’s preservation + the finished Central review report.

---

## Next agent — read these, in order

1. `docs/ai/PC2_2026_08_31_END_OF_DAY_PRESERVATION.md`
2. This file
3. `docs/ai/CENTRAL_UMTUBA_SOCIAL_COMM_PROFILE_INTEGRATION_REVIEW_V1.md` — **FINISHED** (`SOURCE_PASS_MIGRATION_HOLD`)
4. `docs/ai/CURRENT_TASK.md`
5. `docs/DEVELOPMENT_WORKFLOW.md` (migrations: unique versions, no `db push`, no silent renumber)
6. Only if the owner starts the next gate: `docs/ai/PC2_UMTUBA_SOCIAL_COMM_PROFILE_CENTRAL_INTEGRATION_CANDIDATE_V1.md`

Do **not** re-read or re-run:

- Local Supabase environment recovery
- Comms/Rich Profile local completion on `:3000`
- UM Life authoritative gate on `:3001`
- Candidate local product gate on `:3002`
- The Central source/product review

unless `origin/alpha-0.2` is no longer `b5fbeff29cb0f308481b38c06500c572cd44a9c4`.

---

## Live identities (as of 2026-08-31 EOD)

| Role | Value |
| --- | --- |
| Primary checkout (docs only) | `C:\Users\Giga store\Desktop\umtuba\umtuba-web-translation-trunk-port-v1` |
| Primary branch / HEAD | `pc2/umtuba-communications-v1-part1b-identity-discovery` @ `196a0358` (ahead 3) |
| Candidate worktree | `C:\Users\Giga store\Desktop\umtuba\umtuba-web-central-integration-candidate-v1` |
| Candidate branch | `pc2/umtuba-social-comm-profile-central-integration-candidate-v1` (no upstream) |
| Candidate product SHA | `814226776ced7325b174665f773906e163efcb2d` |
| Candidate live HEAD | `75b3896c6a3852258f4e303c4cb54c17d1da5836` (parent = `81422677`) |
| Central base | `origin/alpha-0.2` @ `b5fbeff29cb0f308481b38c06500c572cd44a9c4` |
| UM Life gate worktree | `C:\Users\Giga store\Desktop\umtuba\umtuba-web-um-life-09155b15` detached `09155b15` (clean) |
| discoveryNotFound | `b67a7b33` accepted. `d84dbda5` XOR loser. |

---

## Stack after reboot

Expected last night (all were UP at EOD). After shutdown they will be down.

```text
Docker Desktop          start first
WSL Ubuntu user         giga_store
Local API               http://127.0.0.1:54321
Local DB                127.0.0.1:54322
Local Studio            http://127.0.0.1:54323
Optional Next           :3000 comms, :3001 UM Life, :3002 candidate
```

Start local Supabase from the comms checkout **inside WSL**. Windows `npx supabase status` cannot see Docker. Do not use `status -o env` unless you accept imgproxy/pooler may stop.

```text
CLI_LINKED_HOSTED_REF = tgucwnjwoyeqoxqaxmew
FORBIDDEN = supabase db push, --linked, hosted apply
```

Checkout-local gitignored `.env.local` files already point at `127.0.0.1:54321` + local publishable key. Do not overwrite with hosted. Do not print keys.

TEST_USER_A / TEST_USER_B already exist locally. Reset passwords via local Auth admin only if login fails. Never print passwords.

---

## NEVER PUSH (local-only)

```text
0f89d449   comms   posts precursor + migration filename uniquify
d84dbda5   comms   sync helper XOR loser
196a0358   comms   Messages I18nProvider
75b3896c   candidate welcome-nav fix — keep in later source package
```

Do not `git push` those SHAs or the candidate branch. Do not force-push. Do not merge to `alpha-0.2` / main / shared.

---

## Next required gate (only with owner GO)

```text
TASK_ID = CENTRAL_MIGRATION_VERSION_REALLOCATION_THEN_SOURCE_MERGE_GO
```

Intended sequence (from the finished review — do not skip steps):

1. **Read-only** verify current remote `schema_migrations` (separate GO; not production writes).
2. Allocate **new unique versions** for rich-profile + comms identity **after** that tip. Do **not** reuse `20260915` / `20260916`. In-repo remote map already used `20260915` for Store refund.
3. Keep SQL bodies. Keep original `20260916` `ON CONFLICT (user_id)`. Do **not** replay `0f89d449`.
4. Include `75b3896c` in the source package.
5. Then a **separate** source-merge GO.
6. Then a **separate** targeted migration GO.
7. Then a **separate** deploy GO.

Until those GOs exist:

```text
READY_FOR_SOURCE_MERGE = NO
READY_FOR_MIGRATION_APPLICATION = NO
READY_FOR_PRODUCTION_DEPLOY = NO
```

Do **not** invent Communications Part 2, cover upload, HomeSocialComposer, or a second Facebook-style `/life` feed.

---

## Allowed vs forbidden tomorrow

**Allowed**

- Read docs listed above
- `git fetch --prune` (no merge/rebase/reset/stash/force)
- Confirm SHAs / worktree cleanliness (read-only)
- Restart local Docker + Supabase if the owner wants a live tree
- Docs in the **primary checkout only**
- Start the version-reallocation gate **only if the owner explicitly starts it**

**Forbidden**

- Production / hosted DB writes
- `db push` / `--linked`
- Apply `20260915` / `20260916` (or any realloc copies) to production
- Push of `0f89d449`, `d84dbda5`, `196a0358`, `75b3896c`, or the candidate branch
- Merge to Central / `alpha-0.2` / main
- Modify UM Life worktree product files
- Modify candidate product files unless a named gate + owner GO requires a smallest local fix
- Print secrets / `.env` values
- Re-run finished 2026-08-31 gates “to be sure”

---

## Sync rule

If `git fetch` shows `origin/alpha-0.2` **advanced** past `b5fbeff2`: stop and report. Do not rebase the candidate automatically. Last night’s review assumed that exact base.

If fetch shows divergence on the comms branch: stop. Do not merge/rebase/reset. Those three local commits must stay local.

---

## Security

No production project, auth.users, or hosted rows should be read for write or written. No service-role in the Next app. discoveryNotFound stays generic.
