# PC2 — 2026-08-31 resume

```text
TASK_ID = PC2_UMTUBA_LOCAL_SUPABASE_RUNTIME_ENVIRONMENT_V1
STATUS = FIRMWARE_VTX_DISABLED
DEVICE = PC2
HP_MODEL = HP Z440 Workstation
DATE = 2026-08-31
YESTERDAY = 2026-08-30
PRODUCTION_TOUCHED = NO
REBOOT_INITIATED = NO
BIOS_VTX = DISABLED
```

Owner asked to continue from yesterday. Yesterday’s real stop was end-of-day preservation, not a new product slice.

---

## Where we stopped yesterday

Source: `docs/ai/PC2_2026_08_30_END_OF_DAY_PRESERVATION.md`  
Transcript: [Aug 30 EOD preserve](e8a9c79e-fbee-4eaa-8328-66bc6a13b4d3) (Sunday 2026-08-30 ~20:59 +03)

```text
TASK_ID = PC2_UMTUBA_2026_08_30_END_OF_DAY_PRESERVATION
STATUS = PRESERVED
BIOS_VTX_TOMORROW = REQUIRED
```

Completed yesterday — do **not** repeat:

| Work | SHA / branch |
| --- | --- |
| UM Life Home Entry web build PASS | product `09155b15` / docs tip `ab3f7b03` on `pc2/umtuba-um-life-home-entry-v1` |
| UM Life Home Entry mobile | `4d07bd6` on `pc2/umtuba-um-life-home-entry-v1` |
| Communications Part 1B | product `866749ed` / docs tip `d354fd2f` on `pc2/umtuba-communications-v1-part1b-identity-discovery` |
| Communications mobile | `a660e19` on `pc2/umtuba-communications-v1-part1b-mobile-entry` |
| Rich Profile 2B | tip `455fdca8` on `pc2/um-life-rich-personal-profile-v1-part2b` |
| Windows WSL/Hyper-V features Enabled (NoRestart) | docs only; no Docker / no local DB |

Tomorrow-first-task from that file is today’s work: BIOS VTx → WSL2 → Docker → local Supabase → comms/profile RLS.

---

## Today’s re-check (2026-08-31 15:20 +03)

`git fetch --prune` then `git pull --ff-only`: already up to date. No divergence.

| Check | Result |
| --- | --- |
| Web workspace branch | `pc2/umtuba-communications-v1-part1b-identity-discovery` |
| Web HEAD | `d354fd2fa2a3f16f134acf8038f84b627840498d` (matches origin, 0/0) |
| UM Life web worktree | `ab3f7b03` clean, tracking origin |
| Comms mobile worktree | `a660e19` clean, tracking origin |
| UM Life mobile worktree | `4d07bd6` clean, tracking origin |
| `Win32_Processor.VirtualizationFirmwareEnabled` | **False** |
| systeminfo Virtualization Enabled In Firmware | **No** |
| HypervisorPresent | False |
| LastBootUpTime | 2026-08-01 13:42 (no BIOS/Windows reboot since Aug 30) |
| WSL | still “not installed” |
| Docker Desktop | not on PATH; EXE missing |
| `npx supabase` | 2.116.0 |
| Local ports 54321 / 54322 / 3000 | nothing listening |
| Migrations on disk | `20260915` and `20260916` present; not applied |
| Historical dirty files | left on disk (not deleted, not committed) |

Firmware Intel VT-x is still off. This agent cannot enable it from Windows. Docker installer was **not** downloaded (VTx still off; host-side install is the next step only after Enabled).

---

## Owner physical action (required before any further environment work)

```text
Restart
→ F10 Computer Setup
→ Security
→ System Security
→ Virtualization Technology (VTx) = Enabled
→ F10 save
→ boot Windows
```

If VTx is greyed out: Security → System Security → restore security to factory defaults, then set VTx to Enabled and F10 save.

Do **not** apply `20260915` / `20260916` to production. Do **not** merge to Central / `alpha-0.2`.

---

## After VTx is Enabled (next agent, same TASK_ID)

On this comms checkout only (`866749ed` product / `d354fd2f` docs tip):

1. Confirm `VirtualizationFirmwareEnabled=True` and `HypervisorPresent=True` (or WSL2 running).
2. `wsl --install` / WSL2 runtime ready (supported Microsoft mechanism only).
3. Install Docker Desktop with WSL2 backend. Prefer Docker data on **D:** (~931 GB free historically).
4. `docker version` + `docker info` — engine must be running.
5. `npx supabase start` — confirm TARGET=LOCAL (`http://127.0.0.1:54321`). Never pass production URL.
6. Apply authoritative local history; then validate `20260916` and `20260915` locally only.
7. Create local TEST_USER_A / TEST_USER_B. Run Communications 1B + Rich Profile RLS gates.
8. Point a **separate** Next process at local env. Do not retarget production `.env`.

---

## Security

No production project, auth.users, or comms/profile rows were read or written. No service-role key printed. No commit. No push. No reboot initiated.
