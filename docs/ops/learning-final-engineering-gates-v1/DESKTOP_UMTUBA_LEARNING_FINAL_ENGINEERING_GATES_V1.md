# DESKTOP_UMTUBA_LEARNING_FINAL_ENGINEERING_GATES_V1

```
TASK_ID = DESKTOP_UMTUBA_LEARNING_FINAL_ENGINEERING_GATES_V1
STATUS = FINAL COMPLETE
DATE = 2026-08-24
MACHINE = DESKTOP
OPERATOR = DESKTOP / WEB LEARNING
CANDIDATE_PRESERVED = YES
PRODUCT_CODE_CHANGED_FOR_BUILD_FIX = NO
NODE_MODULES_JUNCTION_ROOT_CAUSE = Worktree node_modules was a Windows junction to the parent repo node_modules. Turbopack rejects a junction that points outside the worktree filesystem root. Parent also had Next 16.2.10 while this lockfile requires 16.2.11.
NODE_MODULES_JUNCTION_FIXED = YES
TYPECHECK = PASS
TARGETED_TESTS = PASS_25
PRODUCTION_BUILD = PASS
LOCAL_SMOKE = PASS
OWNER_APPROVED_DESIGN_PRESERVED = YES
ARABIC_RTL = PASS
DESKTOP_RESPONSIVE = PASS
MOBILE_WEB_RESPONSIVE = PASS
REAL_DATA_CONNECTED = YES_PUBLIC_READ
SUPABASE_ENV_STATUS = AUTHORIZED_PARENT_PUBLIC_KEYS_USED
HISTORICAL_E7C84C66 = UNAVAILABLE_NON_BLOCKING
BRANCH = desktop/learning-approved-design-productization-v1
WORKTREE = C:\Users\1\Desktop\umtuba\umtuba-web\worktrees\DESKTOP-LEARNING-APPROVED-DESIGN-PRODUCTIZATION-V1
COMMIT_CREATED = YES
PUSHED = NO
DEPLOYED = NO
NEW_MIGRATION = NO
MIGRATION_20260934_APPLIED = NO
MOBILE_NATIVE_TOUCHED = NO
LOCAL_PREVIEW_URL = http://localhost:3018/learning
```

Central accepted the productized Learning candidate. This GO closed the remaining engineering gates without redesign.

## Gate 1 — Production build

Removed the worktree `node_modules` junction only (`rmdir` of the junction; parent `node_modules` left intact). Ran `npm ci` in the isolated worktree. Result: real local directory, Next `16.2.11`.

Restored `next.config.ts` and `package-lock.json` to HEAD so no product-code / lockfile workaround was committed for the build.

Cleaned stale `.next` types generated by the old webpack+junction preview (those `.next/types` errors are not product-source failures).

`npx tsc --noEmit` PASS. `npm run build` PASS (Next 16.2.11 Turbopack). Learning routes are in the production route table.

Turbopack warned that the parent lockfile made it infer the parent as workspace root. Build still succeeded. `turbopack.root` was **not** added (product-code change for build is forbidden).

## Gate 2 — Preserve and commit

Commit is created only on `desktop/learning-approved-design-productization-v1` in this isolated worktree. Parent `office/profile-hero-completeness-v1` dirt, android docs, store docs, and other worktrees are not included. `.env.local` is gitignored.

## Gate 3 — Real data / Supabase env

Parent `.env.local` exists and already has authorized Desktop public keys. Key **names** only were inspected. Copied into the worktree `.env.local` (gitignored):

- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`

Not copied: service-role, Livekit, Twilio, or other secrets. Demo flags remain `0`.

Production `next start -p 3018` then showed the live banner (`UMTUBA Learning — live product catalog and routes.`) on `/learning` and `/learning/catalog`. Live course `ja-01` HTTP 200 with real lesson UUIDs. Guest teacher routes correctly go to login because backend env is present.

Authenticated writes / RLS / `20260934` persistence were not exercised. Migration was not applied.

## Gate 4 — Historical e7c84c66

`git fetch origin e7c84c668c251ca6b386a60b2b3c01a89eeb7e1b` → `not our ref`. Object still not fetchable. Current candidate provenance is independently verifiable from `cfc5740` + this commit. Non-blocking.

## Final validation

| Gate | Result |
| --- | --- |
| Targeted vitest | PASS 25 (same Learning files as prior 23, plus `teacherEarnings`) |
| `npx tsc --noEmit` | PASS after cleaning stale `.next` |
| `npm run build` | PASS |
| Production smoke | PASS `http://localhost:3018/learning` |
| Approved design | YES — `learning-visual-root`, dark chrome, guest Discover home |
| Arabic RTL | PASS `lang=ar dir=rtl` + Arabic banner |
| Desktop 1440 / mobile 390 | PASS (new live-data screenshots) |

Prior productization screenshots remain valid for teacher/demo surfaces that the env fix did not restyle. New live-data shots are under `docs/ops/learning-final-engineering-gates-v1/screenshots/`.
