# DESKTOP_ANDROID_RELEASE_ARTIFACT_SEARCH_V1

**DEVICE:** DESKTOP  
**MODE:** SEARCH / INSPECT ONLY  
**DATE:** 2026-08-12  
**IDLE_POLICY:** DO_NOT_IDLE  

## Mission

Physically locate UMTUBA Android release artifacts (`*.aab`, `*.apk`, especially `app-release.aab` / `app-release.apk`) on this Windows Desktop machine without modifying, rebuilding, uploading, or printing secrets.

## Search method

- PowerShell DFS enumeration with skips: `node_modules`, `.git`, `.gradle`, `intermediates`, and similar noise dirs
- Confirmation: `cmd dir /s /b` and `robocopy /L` for `*.aab` / `*.apk` on Desktop, Documents, Downloads, OneDrive
- No Android build started; no files moved/deleted/renamed (report write only under `docs/ops/closeout/`)
- Metadata tools checked: `aapt`, `apksigner`, `jarsigner`, `bundletool`, `java`, Android SDK — **none available** on this machine

## Roots scanned (hits = 0 everywhere)

| Root | Result |
|------|--------|
| `C:\Users\1\Desktop\umtuba\` (all repos/worktrees, depth 25, noise heavy dirs) | 0 AAB/APK |
| `C:\Users\1\Desktop\umtuba\umtuba-mobile\` | 0 AAB/APK |
| `C:\Users\1\Desktop\umtuba\umtuba-web\` | 0 AAB/APK |
| Entire `C:\Users\1\Desktop\` (incl. umtuba-games, UMTUBA_ASSETS, multi-agent, Arabic-named umtuba folder) | 0 AAB/APK |
| `C:\Users\1\Documents\` and `C:\Users\1\Documents\UMTUBA\` | 0 AAB/APK |
| `C:\Users\1\Downloads\` (incl. `umtuba_page_v1`) | 0 AAB/APK |
| `C:\Users\1\OneDrive\` | 0 AAB/APK |
| `C:\Users\1\.expo\`, `C:\Users\1\.cursor\` | 0 AAB/APK |
| Expo/EAS local dirs under AppData / `.eas` | paths missing |
| Common archive/release folders under `Desktop\umtuba\` (`archives`, `archive`, `releases`, `release`) | missing |
| Name filter AppData for `*umtuba*.apk|aab` and `app-release.*` | no hits |
| `**/android/app/build/outputs/**` under Desktop umtuba tree | no `android/` project present |

Not scanned exhaustively: full `C:\` system volume (unnecessary given focused roots + profile confirmation). No D:/E: umtuba Android trees present.

## umtuba-mobile project inspection (supporting evidence)

| Check | Finding |
|-------|---------|
| Repo path | `C:\Users\1\Desktop\umtuba\umtuba-mobile` — **EXISTS** |
| Native `android/` folder | **ABSENT** (`.gitignore` contains `/android`) |
| `android/app/build/outputs/**` | **N/A** — no android project checked out/prebuilt |
| `eas.json` | **ABSENT** |
| `node_modules` | **ABSENT** |
| Android SDK (`%LOCALAPPDATA%\Android\Sdk`, Program Files Android) | **ABSENT** |
| Keystore paths (`*.jks` / `*.keystore` under mobile) | **NONE FOUND** (no android tree) |
| Signing config references | **NONE** (no `build.gradle` / `build.gradle.kts`) |

### Expected identity from `app.config.ts` (gradle/binary not available)

- **PACKAGE_ID (config):** `com.umtuba.app`
- **VERSION_NAME (config):** `1.0.0`
- **VERSION_CODE:** UNKNOWN (not set in inspected Expo config; no Gradle `versionCode`)
- **EAS projectId:** placeholder string in config (`umtuba-mobile-placeholder`) — not a release artifact

`.env` was **not** opened/printed.

## Inventory of found AAB/APK

**NONE.** No candidate files to score for VERSION_*, SIGNED, or GOOGLE_PLAY_UPLOAD_CANDIDATE.

## Ranking / BEST candidate

No files found → no ranking.

## Final metrics

```
ANDROID_RELEASE_FILE_FOUND = NO
BEST_GOOGLE_PLAY_FILE = NONE
AAB_READY_FOR_GOOGLE_PLAY = NO
REBUILD_REQUIRED = YES
```

### Why rebuild required

No release AAB/APK exists on this DESKTOP under practical UMTUBA/Desktop/Documents/Downloads/OneDrive search roots. `umtuba-mobile` is Expo-managed without a local `android/` tree, without EAS config file, without Android SDK/build-tools, and without prior `app-release.aab`/`app-release.apk` outputs. A new signed release AAB must be produced (e.g. Expo prebuild + Gradle bundleRelease, or EAS Build) before Google Play upload is possible on/from this machine.

## Security notes

- No secrets printed
- Keystore: no path existence under mobile android (tree absent)
- Signing tool verification: not applicable (no binaries)

## Open issues / next ops (informational only; not executed)

1. Produce signed `app-release.aab` for `com.umtuba.app` via approved mobile release pipeline
2. Install Android SDK / build-tools (or use EAS cloud build) if building locally on DESKTOP
3. Re-run artifact search after build to capture FULL_PATH + signing verification

---
*Generated by DESKTOP Android release artifact search V1 — inspect only.*
