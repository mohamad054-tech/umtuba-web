-- LOCAL E2E ONLY — helper normally introduced by store product foundation.
create or replace function public.set_row_updated_at()
returns trigger
language plpgsql
as $$
begin
  new.updated_at = now();
  return new;
end;
$$;
