/**
 * Canonical feed video model = DiscoverVideo.
 * Watch maps from this via discoverVideoToWatchVideo (presentation fields only).
 */
export type {
  DiscoverCreator as FeedCreator,
  DiscoverLocation as FeedLocation,
  DiscoverStats as FeedStats,
  DiscoverVideo as FeedVideo,
} from "../../discover/types";
