import type { ReactNode } from "react";

/**
 * Hidden hifz prototype shell — no public chrome links added here.
 */
export default function HifzLayout({ children }: { children: ReactNode }) {
  return children;
}
